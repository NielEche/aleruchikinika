import React from 'react';

const Loading = () => {
    return (
        <div className="flex items-center justify-center min-h-screen bg-black">
            <p>Loading...</p>
            {/* Add any additional loading animations or images here */}
        </div>
    );
};

export default Loading;
